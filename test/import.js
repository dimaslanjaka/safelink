const safelinkify = require('../');

console.log(Object.keys(safelinkify));
const init = new safelinkify.safelink();
/**
 * @type {typeof import('../')['safelink']}
 */
let instance;

//instance.
init.encodeURL();
instance.encodeURL();
